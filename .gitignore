# Environment variables — never commit credentials
.env

# Runtime image cache — ephemeral, large, not version-controlled
data/

# Python
__pycache__/
*.py[cod]
*.pyo
.pytest_cache/
.test-tmp/
.mypy_cache/
.ruff_cache/
.tox/
.nox/
.eggs/
*.egg-info/
.coverage
.coverage.*
coverage.xml
htmlcov/
.venv/
.venv-*/
venv/
env/

# PyInstaller / desktop build artifacts (the .spec under packaging/ IS committed)
build/
dist/
*.manifest
build_log.txt

# Local Claude Code settings — not part of the repo
.claude/
