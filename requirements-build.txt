# Lister-Bridge build-time dependencies for Windows packaging.
#
# Install these beside requirements.txt under the Python 3.12 constraints:
#     python -m pip install -r requirements.txt -r requirements-build.txt -c constraints-py312.txt
#
# streamlit-desktop-app hosts the Streamlit UI in a native window. PyInstaller
# builds the single-file executable from packaging/lister_bridge.spec.

streamlit-desktop-app==0.3.4
pyinstaller==6.21.0
