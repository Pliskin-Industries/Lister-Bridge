# T-003 Handoff

## Outcome

The Windows verification path targets Python 3.12, installs a locked resolved dependency graph, protects the 166-test minimum, and packages a hashed executable locally and in GitHub Actions.

## Changed Files

- `constraints-py312.txt` pins the full Windows and CPython 3.12.10 graph resolved from both requirement sets.
- `requirements-build.txt` pins `streamlit-desktop-app` 0.3.4 and PyInstaller 6.21.0.
- `scripts/verify.ps1` requires Python 3.12 and isolates pytest temporary data inside the repository.
- `.github/workflows/windows-ci.yml` verifies and packages pull requests and pushes to `main` with read-only repository permission and immutable action pins.
- `.gitignore` excludes test, coverage, environment, and supported build caches.

## QA Matrix

| Gate | Reproduction |
| --- | --- |
| AC1 - locked install | Create a clean Python 3.12.10 environment, constrain pip with `python -m pip install pip -c constraints-py312.txt`, install both requirement sets under the same constraint, and run `python -m pip check`. |
| AC2 - protected suite | Run `powershell.exe -NoProfile -ExecutionPolicy Bypass -File scripts/verify.ps1 -PythonExe <python-3.12.10-path>`; confirm exit 0 and at least 166 collected tests. |
| AC3 - executable evidence | Review `.github/workflows/windows-ci.yml`; reproduce `python -m PyInstaller --clean --noconfirm packaging/lister_bridge.spec`, confirm `dist/lister-bridge.exe` is nonempty, and compare its SHA-256 with `dist/lister-bridge.exe.sha256`. Remote artifact verification remains assigned to T-013. |
| AC4 - cache hygiene | Run verification, then `git status --short --untracked-files=all`; confirm no `.pytest_cache`, `build`, `dist`, coverage, egg, tox, nox, mypy, or Ruff cache path appears. |

## Local Evidence

- A disposable Python 3.12.10 environment installed both requirement sets under the complete resolved `constraints-py312.txt` graph; `pip check` reported no broken requirements.
- The canonical `VERIFY` command selected `.venv-py312`, collected 166 tests, passed all 166 tests, and exited 0.
- The primary PyInstaller 6.21.0 build produced a 113,128,225-byte executable under Python 3.12.10.
- That primary executable's SHA-256 was `b7f529244550d9bc6c645cdc8ffa04d3f294b80875ba0fe75f5b532b06f9b6f6`; its generated sidecar matched it.
- Authenticode reports `NotSigned`, consistent with the internal sandbox release scope.
- The protected script rejects Python 3.14.5 with an explicit Python 3.12 requirement.
- `git diff --check` completed without whitespace errors before QA dispatch.

## Risks and Remaining Work

The first verification attempt inherited an inaccessible global pytest temp path. `scripts/verify.ps1` now creates and removes a unique, repository-local `.test-tmp` path after validating that cleanup cannot escape that parent. The first isolated attempt then exposed the missing parent-directory creation; the final script creates it before collection and testing.

The GitHub-hosted run and uploaded artifact cannot be verified locally and remain assigned to T-013. GUI persistence is also outside T-003 and remains assigned to the sandbox release gate.

The build is not claimed to be byte-for-byte deterministic: each run must generate and publish the hash for the artifact it actually produced. Independent QA records its own rebuild size and digest instead of reusing the primary build values.

## Final Gate

- Independent QA: PASS after explicitly superseding two invalid artifact packets caused by overlapping builds.
- Locked graph: 85 installed packages match 85 constraints with zero delta.
- Canonical verification: 166 collected and 166 passed.
- Stable QA artifact: 113,126,255 bytes; SHA-256 `e64bab70a4d4202fd5c3fc9005132198dd63985d8835c7df6e18fda6cd158481`.
- Adversarial review: CLEAR; hosted CI, GUI persistence, signing, and byte-identical output remain unclaimed.

No credentials, `.env` file, AppData application data, or product source was accessed or changed. No push was made.

This changes if the locked graph changes or the hosted T-013 run contradicts the locally verified workflow contract.
